import { part1, part2 } from './dayX';

const testInput = null;

describe('Day X', () => {
  it('should solve test input for part 1', () => {
    expect(part1(testInput)).toEqual(0);
  });

  it('should solve test input for part 2', () => {
    expect(part2(testInput)).toEqual(0);
  });
});
